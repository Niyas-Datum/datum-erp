export interface PWarehouseModel{
      id: number;
      name: string;
      code: string;
}
export interface WarehouseTypes{
      id: number;
      name: string;
      code: string;
}
export interface LocationType{
      ID:number;
      LocationType:string;
}